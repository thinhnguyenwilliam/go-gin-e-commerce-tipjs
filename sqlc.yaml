version: "2"
sql:
  - engine: "mysql"
    schema: "db/schema/"        # nơi có goose migration (chỉ chứa CREATE TABLE)
    queries: "db/queries/"      # nơi có query (phải chứa file .sql riêng)
    gen:
      go:
        package: "db"
        out: "internal/db"
